services:
  - type: web
    name: narrativeos-bridge
    runtime: docker
    branch: main
    rootDir: infra/superplane
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: "8765"
    healthCheckPath: /health
